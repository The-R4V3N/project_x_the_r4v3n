
cmake_minimum_required(VERSION 3.10)

project(project_x, CXX)

# include_directories(${CAN_INCLUDE_DIR})
# include_directories(${})

set(sources main.cpp)

add_executable (project_x_cmake ${sources})



